set(PROG treeadd-extra)
list(APPEND CPPFLAGS -DTORONTO)
if(DEFINED SMALL_PROBLEM_SIZE)
  set(RUN_OPTIONS 20)
else()
  set(RUN_OPTIONS 24 1)
  if(ARCH STREQUAL "XCore")
    set(XCORE_TARGET_NEEDS_MEMORY 128)
  endif()
endif()
llvm_multisource()
